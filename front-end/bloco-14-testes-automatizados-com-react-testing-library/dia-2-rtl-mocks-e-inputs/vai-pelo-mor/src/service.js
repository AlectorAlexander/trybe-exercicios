
const numeroAleatorio = () => {
   return Math.floor(Math.random() * 100)
};

const caixaAlta = (param) => {
const upperCase = param.toUpperCase();
return upperCase;
}

const firsto = (param) => {
   const firstThingsFuckingFirst = param[0]
   return firstThingsFuckingFirst;
}

const amorAiLoveU = (param1, param2) => {
   const siameses = [param1, param2];
   return siameses;
}

export default caixaAlta;
